# Empty uses same-origin /api; point to an approved service when mocks are off.
VITE_API_BASE_URL=

# Development only: on (default) or off. Production never starts MSW.
VITE_MOCK_MODE=on

# success, empty, validation-error, server-error, retry-once
VITE_MOCK_SCENARIO=success
